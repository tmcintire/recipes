import React from 'react';

/* Home Component
* This component is the primary component the user will land on.
* Ordinarily this component as is would be written as a pure function, but in
* my case I will be adding more functionality to it */

export class Home extends React.Component {
  render() {
    return (
      <div>
        <h1>Home page</h1>
      </div>
    );
  }
}
